<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kevin.mcp.mapper.OrgRegionMapper">

    <!-- 映射数据库字段与实体属性 -->
    <resultMap id="OrgRegionResultMap" type="com.kevin.mcp.entity.OrgRegion">
        <id column="id" property="id"/>
        <result column="company_id" property="companyId"/>
        <result column="parent_id" property="parentId"/>
        <result column="name" property="name"/>
        <result column="level" property="level"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
    </resultMap>

    <!-- 复用基础查询列 -->
    <sql id="Base_Column_List">
        id,
        company_id,
        parent_id,
        name,
        level,
        sort_order,
        status
    </sql>

    <!-- 根据主键查询单条记录 -->
    <select id="selectById" resultMap="OrgRegionResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM org_region
        WHERE id = #{id}
    </select>

    <!-- 按非空条件查询记录列表 -->
    <select id="selectByCondition" resultMap="OrgRegionResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM org_region
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="companyId != null">
                AND company_id = #{companyId}
            </if>
            <if test="parentId != null">
                AND parent_id = #{parentId}
            </if>
            <if test="name != null and name != ''">
                AND name = #{name}
            </if>
            <if test="level != null">
                AND level = #{level}
            </if>
            <if test="sortOrder != null">
                AND sort_order = #{sortOrder}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- 按非空条件统计记录数量 -->
    <select id="countByCondition" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM org_region
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="companyId != null">
                AND company_id = #{companyId}
            </if>
            <if test="parentId != null">
                AND parent_id = #{parentId}
            </if>
            <if test="name != null and name != ''">
                AND name = #{name}
            </if>
            <if test="level != null">
                AND level = #{level}
            </if>
            <if test="sortOrder != null">
                AND sort_order = #{sortOrder}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- 动态新增非空字段，主键回填到实体 -->
    <insert id="insert" parameterType="com.kevin.mcp.entity.OrgRegion" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO org_region
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="companyId != null">
                company_id,
            </if>
            <if test="parentId != null">
                parent_id,
            </if>
            <if test="name != null and name != ''">
                name,
            </if>
            <if test="level != null">
                level,
            </if>
            <if test="sortOrder != null">
                sort_order,
            </if>
            <if test="status != null">
                status,
            </if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="companyId != null">
                #{companyId},
            </if>
            <if test="parentId != null">
                #{parentId},
            </if>
            <if test="name != null and name != ''">
                #{name},
            </if>
            <if test="level != null">
                #{level},
            </if>
            <if test="sortOrder != null">
                #{sortOrder},
            </if>
            <if test="status != null">
                #{status},
            </if>
        </trim>
    </insert>

    <!-- 根据主键动态更新非空字段 -->
    <update id="updateById" parameterType="com.kevin.mcp.entity.OrgRegion">
        UPDATE org_region
        <trim prefix="SET" suffixOverrides=",">
            <if test="companyId != null">
                company_id = #{companyId},
            </if>
            <if test="parentId != null">
                parent_id = #{parentId},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="level != null">
                level = #{level},
            </if>
            <if test="sortOrder != null">
                sort_order = #{sortOrder},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </trim>
        WHERE id = #{id}
    </update>

    <!-- 根据主键删除单条记录 -->
    <delete id="deleteById">
        DELETE FROM org_region
        WHERE id = #{id}
    </delete>
</mapper>
